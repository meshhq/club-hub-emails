export interface ClubInfo {
    name: string
    shortName: string
    baseURL: string
    website: string
    logoURL: string,
    twitterURL?: string,
    facebookURL?: string,
    instagramURL?: string,
    street: string,
    city: string,
    state: string,
    zip: string,
    unsubscribeURL: string
}
